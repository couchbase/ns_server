case os:getenv("NS_SERVER_VERSION") of
    false -> CONFIG;
    [] -> CONFIG;
    Version ->
        [{application, ns_server, Props}] = CONFIG,
        NewProps = lists:keystore(vsn, 1, Props, {vsn, Version}),
        [{application, ns_server, NewProps}]
end.
