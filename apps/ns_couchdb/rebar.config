{erl_opts, [bin_info_opt, nowarn_deprecated_type,
            %% Paths need to be relative to the top level of ns_server
            {i, "apps/ns_server/include"},
            {i, "../couchdb/src/"},
            {i, "../couchdb/src/couchdb"},
            {parse_transform, ale_transform}]}.
